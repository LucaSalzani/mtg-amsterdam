{
  "name": "mtg-amsterdam-planner",
  "version": "1.0.0",
  "private": true,
  "description": "MagicCon Amsterdam event scraper and overlap calendar.",
  "type": "module",
  "scripts": {
    "scrape": "node scripts/scrape-schedule.mjs",
    "build:data": "node scripts/build-events.mjs",
    "serve": "node scripts/serve.mjs",
    "build": "npm run scrape && npm run build:data"
  },
  "dependencies": {
    "playwright": "^1.54.2"
  }
}
